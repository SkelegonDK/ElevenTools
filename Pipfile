[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
streamlit = ">=1.32.0"
black = ">=24.0.0"
openai = ">=1.12.0"
urllib3 = ">=2.2.0"
pytest = ">=8.0.0"
pyperclip = ">=1.8.2"
elevenlabs = ">=0.3.0"
requests = ">=2.31.0"
python-dotenv = ">=1.0.0"
pydantic = ">=2.6.0"
pytest-cov = ">=4.1.0"

[dev-packages]
flake8 = ">=7.0.0"
mypy = ">=1.8.0"
isort = ">=5.13.0"

[requires]
python_version = "3.12"
